{
  "devDependencies": {
    "@reactpreview/cli": "0.7.2",
    "concurrently": "7.6.0",
    "eslint": "8.31.0",
    "husky": "8.0.2",
    "npm-check-updates": "16.6.2",
    "npm-run-all": "^4.1.5",
    "turbo": "1.6.3",
    "typescript": "4.9.4",
    "vite-node": "^0.27.1",
    "zx": "7.1.1"
  },
  "scripts": {
    "upd": "pnpm add -g @pnpm/exe; pnpm turbo upd",
    "types": "pnpm turbo types",
    "lint": "pnpm turbo lint",
    "build": "pnpm turbo build --filter=!@srtp/wurkspace",
    "test": "pnpm turbo test --filter=!@srtp/wurkspace ",
    "dev": "pnpm turbo dev --filter=!@srtp/wurkspace",
    "types:libs": "pnpm turbo types --filter=./libs/*",
    "lint:libs": "pnpm turbo lint --filter=./libs/*",
    "build:libs": "pnpm turbo build --filter=./libs/*",
    "test:libs": "pnpm turbo test --filter=./libs/*",
    "dev:libs": "pnpm turbo dev --filter=./libs/*",
    "clean": "pnpm turbo clean",
    "ci": "pnpm turbo types lint",
    "prepare": "husky install"
  }
}
